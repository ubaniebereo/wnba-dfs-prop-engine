#!/bin/bash
# Double-click this file in Finder to start the WNBA Prop Engine dashboard.
# It opens your browser automatically. Close the Terminal window to stop it.
cd "/Users/mr.o/WNBA BET PROJECT" || exit 1
exec ./.venv/bin/python start.py
